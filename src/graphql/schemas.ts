import { commentsSchema } from "../comments/comments.schema";
import { tweetsSchema } from "../tweets/tweets.schema";
import { usersSchema } from "./../users/users.schema";
export default [usersSchema, tweetsSchema, commentsSchema];
